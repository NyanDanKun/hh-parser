# 🎯 Как запустить HH Parser

## Самый простой способ:

```bash
start.bat
```

Готово! Откроется браузер на http://localhost:5000

---

## Если что-то пошло не так:

### 1. Python не найден?
```bash
# Проверь версию
python --version
# или
py --version

# Должно быть 3.8+
```

### 2. Ошибка импорта модулей?
```bash
# Установи зависимости
pip install -r requirements.txt
```

### 3. Порт 5000 занят?
```bash
# Закрой другое приложение на порту 5000
# Или измени порт в app.py (строка 318)
```

### 4. База данных заблокирована?
```bash
# Закрой все процессы Python и перезапусти
```

---

## Полезные команды:

```bash
# Запуск
py run.py

# Просмотр базы данных
py -c "import sqlite3; conn = sqlite3.connect('data/vacancies.db'); print(conn.execute('SELECT COUNT(*) FROM vacancies').fetchone())"

# Очистка базы (осторожно!)
del data\vacancies.db

# Просмотр логов
# (выводятся в терминал при запуске)
```

---

## Структура проекта:

```
hh-parser/
├── start.bat           ← Запуск одной кнопкой
├── run.py             ← Главный файл запуска
├── app.py             ← Flask приложение
├── requirements.txt   ← Зависимости
├── config/
│   └── config.yaml    ← Настройки API
├── src/               ← Модули парсера
├── templates/         ← HTML страницы
├── static/            ← CSS/JS файлы
└── data/              ← База данных SQLite
```

---

## Типичные сценарии:

### Первый запуск:
1. Открой терминал в папке проекта
2. `pip install -r requirements.txt`
3. `py run.py`
4. Открой http://localhost:5000

### Обычный запуск:
1. Двойной клик на `start.bat`
2. Или в терминале: `py run.py`

### Остановка:
- Нажми `Ctrl+C` в терминале

---

## Порты:

- **5000** - HH Parser (этот проект)
- **5001** - Auto-ID Parser (если запущен)
- **5002+** - Другие сервисы

Если порт занят - просто измени в `app.py` последней строке.
