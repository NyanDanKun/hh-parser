# 📊 HH.ru Parser

Веб-дашборд для анализа вакансий с HeadHunter.ru

---

## 🚀 Быстрый старт

```bash
# Windows - двойной клик или:
start.bat

# Или вручную:
py run.py
```

**Откроется:** http://localhost:5000

---

## 📦 Первая установка

```bash
# 1. Установить зависимости
pip install -r requirements.txt

# 2. Запуск
py run.py
```

---

## 💡 Возможности

- **Сбор вакансий** - парсинг через HH.ru API
- **Система проектов** - разные поиски отдельно
- **Анализ ключевых слов** - топ-навыки и требования
- **Статистика зарплат** - средние значения и диапазоны
- **Графики** - визуализация данных
- **Фильтры** - по зарплате и опыту
- **Экспорт** - JSON/CSV выгрузка

---

## 📁 Структура

```
hh-parser/
├── start.bat           ← Запуск одной кнопкой
├── run.py             ← Главный файл
├── app.py             ← Flask backend
├── requirements.txt   ← Зависимости
├── config/
│   └── config.yaml    ← Настройки API
├── src/               ← Модули парсера
│   ├── api_client.py  
│   ├── parser.py
│   ├── analyzer.py
│   └── storage.py
├── templates/         ← HTML
├── static/            ← CSS/JS
└── data/              ← SQLite база
```

---

## 🛠️ Как использовать

### 1. Запуск
```bash
start.bat  # или py run.py
```

### 2. Создать проект
- Открой http://localhost:5000
- "Настройки и сбор"
- Поставь галочку "Создать новый проект"
- Введи название и параметры
- "Начать сбор"

### 3. Просмотр результатов
- Переключайся между проектами
- Смотри графики и статистику
- Экспортируй в JSON/CSV

---

## ⚙️ Конфигурация

`config/config.yaml`:
```yaml
api:
  base_url: https://api.hh.ru
  user_agent: HH-Parser/1.0
  
search:
  query: "руководитель отдела маркетинга"
  area: 1    # 1=Москва, 2=СПб, 113=Россия
  max_pages: 10
  per_page: 20
```

---

## 🔧 Troubleshooting

### Python не найден?
```bash
py --version  # Должно быть 3.8+
```

### Порт 5000 занят?
Измени в `app.py` последней строке:
```python
app.run(port=5001)  # Любой свободный порт
```

### База заблокирована?
```bash
# Закрой все процессы Python и перезапусти
```

### Подробности
Смотри `HOW_TO_RUN.md` для детальных инструкций

---

## 📊 API Endpoints

- `GET /` - Главная страница
- `GET /api/stats` - Статистика
- `GET /api/projects` - Список проектов
- `POST /api/collect` - Запуск сбора
- `GET /api/export/json` - Экспорт JSON
- `GET /api/export/csv` - Экспорт CSV

---

## 🔗 Интеграция

См. `INTEGRATION.md` для подключения к gateway и другим сервисам

---

## 📝 Лицензия

MIT

---

**Создано для анализа рынка труда и оптимизации резюме**
